<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Sitio Web Completo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- ===== ESTILOS CSS ===== -->
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            line-height: 1.6;
        }

        header {
            background-color: #1e1e2f;
            color: white;
            padding: 30px;
            text-align: center;
        }

        nav {
            background-color: #2d2d44;
            padding: 15px;
        }

        nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }

        nav ul li a:hover {
            color: #00d4ff;
        }

        main {
            padding: 30px;
        }

        section {
            margin-bottom: 40px;
            background: white;
            padding: 20px;
            border-radius: 8px;
        }

        h2 {
            margin-bottom: 15px;
            color: #1e1e2f;
        }

        p {
            margin-bottom: 10px;
        }

        /* ===== FORMULARIO ===== */
        form {
            max-width: 500px;
        }

        label {
            display: block;
            margin-top: 15px;
        }

        input, textarea, select {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
        }

        button {
            margin-top: 20px;
            padding: 12px;
            background-color: #1e1e2f;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #00d4ff;
            color: black;
        }

        /* ===== TABLA ===== */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        table th, table td {
            padding: 12px;
            border: 1px solid #ccc;
            text-align: center;
        }

        table th {
            background-color: #1e1e2f;
            color: white;
        }

        /* ===== FOOTER ===== */
        footer {
            background-color: #1e1e2f;
            color: white;
            text-align: center;
            padding: 20px;
        }

        .hidden {
            display: none;
        }
    </style>
</head>

<body>

<!-- ===== ENCABEZADO ===== -->
<header>
    <h1>Mi Sitio Web Profesional</h1>
    <p>Ejemplo de HTML largo con CSS y JavaScript</p>
</header>

<!-- ===== MENÚ ===== -->
<nav>
    <ul>
        <li><a href="#inicio">Inicio</a></li>
        <li><a href="#servicios">Servicios</a></li>
        <li><a.pista="aHR0cHM6Ly93d3cud2lraXBlZGlhLm9yZw"
        <li><a href="#registro">Registro</a></li>
        <li><a href="#usuarios">Usuarios</a></li>
        <li><a href="#contacto">Contacto</a></li>
    </ul>
</nav>

<!-- ===== CONTENIDO ===== -->
<main>

    <!-- SECCIÓN INICIO -->
    <section id="inicio">
        <h2>Inicio</h2>
        <p>
            Este documento es un ejemplo de un archivo HTML extenso que incluye
            estructura semántica, estilos CSS internos y funcionalidades JavaScript.
        </p>
        <p>
            Ideal para prácticas, proyectos escolares o demostraciones.
        </p>
    </section>

    <!-- SECCIÓN SERVICIOS -->
    <section id="servicios">
        <h2>Servicios</h2>
        <ul>
            <li>Desarrollo Web</li>
            <li>Diseño UI/UX</li>
            <li>Soporte Técnico</li>
            <li>Consultoría Digital</li>
        </ul>
    </section>

    <!-- SECCIÓN FORMULARIO -->
    <section id="registro">
        <h2>Formulario de Registro</h2>

        <form id="formRegistro">
            <label>Nombre completo</label>
            <input type="text" id="nombre" required>

            <label>Email</label>
            <input type="email" id="email" required>

            <label>Edad</label>
            <input type="number" id="edad" min="1">

            <label>Rol</label>
            <select id="rol">
                <option>Usuario</option>
                <option>Administrador</option>
            </select>

            <label>Comentarios</label>
            <textarea id="comentarios"></textarea>

            <button type="submit">Enviar</button>
        </form>

        <p id="mensaje" class="hidden">Registro enviado correctamente ✔</p>
    </section>

    <!-- SECCIÓN TABLA -->
    <section id="usuarios">
        <h2>Lista de Usuarios</h2>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Rol</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>001</td>
                    <td>Ana</td>
                    <td>Admin</td>
                    <td>Activo</td>
                </tr>
                <tr>
                    <td>002</td>
                    <td>Carlos</td>
                    <td>Usuario</td>
                    <td>Inactivo</td>
                </tr>
            </tbody>
        </table>
    </section>

    <!-- SECCIÓN CONTACTO -->
    <section id="contacto">
        <h2>Contacto</h2>
        <p>Email: contacto@ejemplo.com</p>
        <p>Teléfono: +00 123 456 789</p>
    </section>

</main>

<!-- ===== PIE DE PÁGINA ===== -->
<footer>
    <p>© 2025 - Todos los derechos reservados</p>
</footer>

<!-- ===== JAVASCRIPT ===== -->
<script>
    const form = document.getElementById("formRegistro");
    const mensaje = document.getElementById("mensaje");

    form.addEventListener("submit", function(event) {
        event.preventDefault();

        mensaje.classList.remove("hidden");

        setTimeout(() => {
            mensaje.classList.add("hidden");
            form.reset();
        }, 3000);
    });
</script>

</body>
</html>
